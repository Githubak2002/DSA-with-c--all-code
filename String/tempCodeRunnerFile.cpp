 // left = i-1,right = i;
        // while(s.at(left) == s.at(right) && left>=0 && right<n)
        // {
        //     right++;
        //     left--;
        // }
        // if(maxLen < right-left-1)
        // {
        //     maxLen = right-left-1;
        //     pal = s.substr(left+1,maxLen);
        // }
        // cout<<"i: "<<i<<endl;